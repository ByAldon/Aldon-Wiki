import React from 'react';

// This component is intentionally left blank.
// All editing functionality has been removed as per user request
// to make the wiki a static, read-only site.
const EditView: React.FC = () => {
  return null;
};

export default EditView;
